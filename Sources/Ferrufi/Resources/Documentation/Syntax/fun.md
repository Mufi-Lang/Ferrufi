# fun

Define a reusable function.

## Syntax
```mufi
fun name(parameters) {
    // code
    return value;
}
```

## Examples
```mufi
fun add(a, b) {
    return a + b;
}

var result = add(5, 10);
```
