# len

Length of a collection.

## Syntax
`len(collection);`

## Description
Returns the number of elements in a list, vector, or hash table.

## Examples
```mufi
var v = {1, 2, 3};
print(len(v)); // Output: 3
```
