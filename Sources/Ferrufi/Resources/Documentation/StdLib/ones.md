# ones

Create a matrix of ones.

## Syntax
`A = ones(m, n)`

## Description
`ones(m, n)` returns an `m`-by-`n` matrix of ones.

## Examples
Create a 2-by-3 matrix of ones:
```mufi
var A = ones(2, 3);
print(A);
```
