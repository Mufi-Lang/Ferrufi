# zeros

Create a matrix of zeros.

## Syntax
`A = zeros(m, n)`

## Description
`zeros(m, n)` returns an `m`-by-`n` matrix of zeros.

## Examples
Create a 3-by-2 matrix of zeros:
```mufi
var A = zeros(3, 2);
print(A);
```
