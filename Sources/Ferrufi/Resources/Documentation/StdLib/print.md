# Standard Library: print

The `print` function outputs text to the standard output (usually the REPL or console).

## Signature
`func print(value: Any)`

## Parameters
- `value`: The value to be printed. Can be a string, number, or any other Mufi type.

## Examples
```mufi
print("Hello, Mufi!")
print(42)
```
